# SignalNest uses split environment files.
#
# Frontend:
#   frontend/.env.example frontend/.env
#
# Backend:
#   backend/.env.example backend/.env
#
# Keep server-only secrets, payment credentials, database URLs, and JWT
# secrets out of frontend/.env. Vite exposes VITE_* variables to the browser.
