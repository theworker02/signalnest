# Infrastructure

This directory is reserved for production infrastructure definitions such as Terraform, Helm charts, or cloud deployment manifests.

The current local deployment path is `docker-compose.yml` plus `nginx/default.conf`.
